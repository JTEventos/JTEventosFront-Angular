import { EventTypeLookupPipe } from './event-type-lookup.pipe';

describe('EventTypeLookupPipe', () => {
  it('create an instance', () => {
    const pipe = new EventTypeLookupPipe();
    expect(pipe).toBeTruthy();
  });
});
