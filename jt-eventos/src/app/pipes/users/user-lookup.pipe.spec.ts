import { UserLookupPipe } from './user-lookup.pipe';

describe('UserLookupPipe', () => {
  it('create an instance', () => {
    const pipe = new UserLookupPipe();
    expect(pipe).toBeTruthy();
  });
});
